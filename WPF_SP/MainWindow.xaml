<Window x:Class="WPF_SP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WPF_SP.ViewModels"
        mc:Ignorable="d"
        Title="Sistema de Mantenimiento - NeptunoDB" Height="600" Width="1000">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    
    <Window.Resources>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="Black" />
        </Style>
    </Window.Resources>

    <Grid>
        <TabControl Margin="10">
            <!-- TAB 1: CATEGORIAS -->
            <TabItem Header="Categorías">
                <Grid DataContext="{Binding CategoriasVM}" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    
                    <DataGrid Grid.Column="0" ItemsSource="{Binding Categorias}" SelectedItem="{Binding CategoriaSeleccionada}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,10,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding CategoriaID}" />
                            <DataGridTextColumn Header="Nombre" Binding="{Binding NombreCategoria}" />
                            <DataGridTextColumn Header="Descripción" Binding="{Binding Descripcion}" />
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Mantenimiento de Categoría" FontWeight="Bold" Margin="0,0,0,10" />
                        <TextBlock Text="Nombre de Categoría:" />
                        <TextBox Text="{Binding CategoriaSeleccionada.NombreCategoria, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" />
                        
                        <TextBlock Text="Descripción:" />
                        <TextBox Text="{Binding CategoriaSeleccionada.Descripcion, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Height="60" TextWrapping="Wrap" AcceptsReturn="True" />
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="Nuevo" Command="{Binding NuevoCommand}" Width="70" Margin="5" />
                            <Button Content="Guardar" Command="{Binding GuardarCommand}" Width="70" Margin="5" />
                            <Button Content="Eliminar" Command="{Binding EliminarCommand}" Width="70" Margin="5" Background="LightCoral" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </TabItem>

            <!-- TAB 2: PROVEEDORES -->
            <TabItem Header="Proveedores">
                <Grid DataContext="{Binding ProveedoresVM}" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Buscar Contacto:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox Text="{Binding BusquedaNombre, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,15,0" />
                        
                        <TextBlock Text="Ciudad:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox Text="{Binding BusquedaCiudad, UpdateSourceTrigger=PropertyChanged}" Width="150" Margin="0,0,15,0" />
                        
                        <Button Content="Buscar" Command="{Binding BuscarCommand}" Width="80" Margin="0,0,5,0" />
                        <Button Content="Limpiar" Command="{Binding LimpiarBusquedaCommand}" Width="80" />
                    </StackPanel>
                    
                    <DataGrid Grid.Row="1" Grid.Column="0" ItemsSource="{Binding Proveedores}" SelectedItem="{Binding ProveedorSeleccionado}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,10,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ProveedorID}" />
                            <DataGridTextColumn Header="Compañía" Binding="{Binding CompaniaNombre}" />
                            <DataGridTextColumn Header="Contacto" Binding="{Binding NombreContacto}" />
                            <DataGridTextColumn Header="Cargo" Binding="{Binding CargoContacto}" />
                            <DataGridTextColumn Header="Ciudad" Binding="{Binding Ciudad}" />
                            <DataGridTextColumn Header="País" Binding="{Binding Pais}" />
                            <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" />
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <ScrollViewer Grid.Row="1" Grid.Column="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Datos del Proveedor" FontWeight="Bold" Margin="0,0,0,10" />
                            <TextBlock Text="Compañía:" />
                            <TextBox Text="{Binding ProveedorSeleccionado.CompaniaNombre, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Contacto:" />
                            <TextBox Text="{Binding ProveedorSeleccionado.NombreContacto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Cargo:" />
                            <TextBox Text="{Binding ProveedorSeleccionado.CargoContacto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Dirección:" />
                            <TextBox Text="{Binding ProveedorSeleccionado.Direccion, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Ciudad:" />
                            <TextBox Text="{Binding ProveedorSeleccionado.Ciudad, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Código Postal:" />
                            <TextBox Text="{Binding ProveedorSeleccionado.CodigoPostal, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="País:" />
                            <TextBox Text="{Binding ProveedorSeleccionado.Pais, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Teléfono:" />
                            <TextBox Text="{Binding ProveedorSeleccionado.Telefono, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                                <Button Content="Nuevo" Command="{Binding NuevoCommand}" Width="70" Margin="5" />
                                <Button Content="Guardar" Command="{Binding GuardarCommand}" Width="70" Margin="5" />
                                <Button Content="Eliminar" Command="{Binding EliminarCommand}" Width="70" Margin="5" Background="LightCoral" />
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- TAB 3: PRODUCTOS -->
            <TabItem Header="Productos">
                <Grid DataContext="{Binding ProductosVM}" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    
                    <DataGrid Grid.Column="0" ItemsSource="{Binding Productos}" SelectedItem="{Binding ProductoSeleccionado}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,10,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding ProductoID}" />
                            <DataGridTextColumn Header="Producto" Binding="{Binding NombreProducto}" />
                            <DataGridTextColumn Header="Prov. ID" Binding="{Binding ProveedorID}" />
                            <DataGridTextColumn Header="Cat. ID" Binding="{Binding CategoriaID}" />
                            <DataGridTextColumn Header="Precio" Binding="{Binding PrecioUnidad, StringFormat=C}" />
                            <DataGridTextColumn Header="Stock" Binding="{Binding UnidadesEnExistencia}" />
                            <DataGridCheckBoxColumn Header="Descontinuado" Binding="{Binding Descontinuado}" />
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Datos del Producto" FontWeight="Bold" Margin="0,0,0,10" />
                            
                            <TextBlock Text="Nombre del Producto:" />
                            <TextBox Text="{Binding ProductoSeleccionado.NombreProducto, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="ID Proveedor:" />
                            <TextBox Text="{Binding ProductoSeleccionado.ProveedorID, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="ID Categoría:" />
                            <TextBox Text="{Binding ProductoSeleccionado.CategoriaID, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Cantidad por Unidad:" />
                            <TextBox Text="{Binding ProductoSeleccionado.CantidadPorUnidad, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Precio:" />
                            <TextBox Text="{Binding ProductoSeleccionado.PrecioUnidad, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Stock (Unidades en Existencia):" />
                            <TextBox Text="{Binding ProductoSeleccionado.UnidadesEnExistencia, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <CheckBox Content="Descontinuado" IsChecked="{Binding ProductoSeleccionado.Descontinuado, UpdateSourceTrigger=PropertyChanged}" Margin="0,10,0,10" />
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Nuevo" Command="{Binding NuevoCommand}" Width="70" Margin="5" />
                                <Button Content="Guardar" Command="{Binding GuardarCommand}" Width="70" Margin="5" />
                                <Button Content="Eliminar" Command="{Binding EliminarCommand}" Width="70" Margin="5" Background="LightCoral" />
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- TAB 4: PEDIDOS -->
            <TabItem Header="Pedidos">
                <Grid DataContext="{Binding PedidosVM}" Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="1*" />
                    </Grid.ColumnDefinitions>
                    
                    <DataGrid Grid.Column="0" ItemsSource="{Binding Pedidos}" SelectedItem="{Binding PedidoSeleccionado}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,10,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Pedido ID" Binding="{Binding PedidoID}" />
                            <DataGridTextColumn Header="Cliente ID" Binding="{Binding ClienteID}" />
                            <DataGridTextColumn Header="Emp. ID" Binding="{Binding EmpleadoID}" />
                            <DataGridTextColumn Header="Fecha Pedido" Binding="{Binding FechaPedido, StringFormat=d}" />
                            <DataGridTextColumn Header="Fecha Envío" Binding="{Binding FechaEnvio, StringFormat=d}" />
                            <DataGridTextColumn Header="Destinatario" Binding="{Binding Destinatario}" />
                            <DataGridTextColumn Header="País" Binding="{Binding PaisDestino}" />
                        </DataGrid.Columns>
                    </DataGrid>
                    
                    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Text="Datos del Pedido" FontWeight="Bold" Margin="0,0,0,10" />
                            
                            <TextBlock Text="Cliente ID:" />
                            <TextBox Text="{Binding PedidoSeleccionado.ClienteID, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Empleado ID:" />
                            <TextBox Text="{Binding PedidoSeleccionado.EmpleadoID, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Fecha Pedido:" />
                            <DatePicker SelectedDate="{Binding PedidoSeleccionado.FechaPedido, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Fecha Requerida:" />
                            <DatePicker SelectedDate="{Binding PedidoSeleccionado.FechaRequerida, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Fecha Envío:" />
                            <DatePicker SelectedDate="{Binding PedidoSeleccionado.FechaEnvio, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Vía (Transportista ID):" />
                            <TextBox Text="{Binding PedidoSeleccionado.TransportistaID, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Destinatario:" />
                            <TextBox Text="{Binding PedidoSeleccionado.Destinatario, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="Ciudad Destino:" />
                            <TextBox Text="{Binding PedidoSeleccionado.CiudadDestino, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <TextBlock Text="País Destino:" />
                            <TextBox Text="{Binding PedidoSeleccionado.PaisDestino, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,5" />
                            
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                                <Button Content="Nuevo" Command="{Binding NuevoCommand}" Width="70" Margin="5" />
                                <Button Content="Guardar" Command="{Binding GuardarCommand}" Width="70" Margin="5" />
                                <Button Content="Eliminar" Command="{Binding EliminarCommand}" Width="70" Margin="5" Background="LightCoral" />
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- TAB 5: REPORTES -->
            <TabItem Header="Reportes">
                <Grid DataContext="{Binding ReportesVM}" Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                        <TextBlock Text="Fecha Inicio:" VerticalAlignment="Center" Margin="0,0,5,0" />
                        <DatePicker SelectedDate="{Binding FechaInicio, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,15,0" />
                        
                        <TextBlock Text="Fecha Fin:" VerticalAlignment="Center" Margin="0,0,5,0" />
                        <DatePicker SelectedDate="{Binding FechaFin, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,15,0" />
                        
                        <Button Content="Generar Reporte" Command="{Binding GenerarReporteCommand}" Width="120" />
                    </StackPanel>
                    
                    <DataGrid Grid.Row="1" ItemsSource="{Binding Reporte}" AutoGenerateColumns="False" IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Pedido ID" Binding="{Binding PedidoID}" />
                            <DataGridTextColumn Header="Fecha" Binding="{Binding FechaPedido, StringFormat=d}" />
                            <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" />
                            <DataGridTextColumn Header="Producto" Binding="{Binding Producto}" />
                            <DataGridTextColumn Header="Precio Und." Binding="{Binding PrecioUnidad, StringFormat=C}" />
                            <DataGridTextColumn Header="Cant." Binding="{Binding Cantidad}" />
                            <DataGridTextColumn Header="Descuento %" Binding="{Binding Descuento}" />
                            <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C}" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
